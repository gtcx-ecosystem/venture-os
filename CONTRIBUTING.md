# Contributing — Venture OS

## Quick start

```bash
pnpm install
pnpm dev
```

App entry: `app/` · Product docs: `product-spec.md`.

## Commit convention

Use conventional commits: `type(scope): subject` (lowercase, imperative).

Examples: `feat(app): add venture profile card`, `docs(readme): clarify dev commands`.
