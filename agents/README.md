# agents/ — venture-os harness

Fleet persona profiles: `../bridge-os/pm/spec/repo-persona-profiles.json`

Required: `bootstrap/` · `capabilities/` · `playbooks/` · `personas/` · `integrations/`

Agent startup, evidence, and runtime operating instructions are separated from product docs and live under `docs/operations/agent-init/`. This folder is the repo-root agent harness and is validated against `pm/spec/agents-pack.json`.
