# =============================================================================
# REFERENCE ONLY — do not kubectl apply from venture-os
# =============================================================================
# Canonical overlay: fabric-os/deploy/kubernetes/overlays/staging/venture-os/
# See deploy/kubernetes/README.md
# =============================================================================
