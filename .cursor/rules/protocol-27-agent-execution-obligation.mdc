---
description: Protocol 27 — Agent Execution Obligation (run commands; diagnose before delegating)
globs: ["**/*"]
alwaysApply: true
---

# Protocol 27: Agent Execution Obligation (AGENT-EXEC-OBL)

> **Normative:** `gtcx-docs/governance/protocols/27-agent-execution-obligation/protocol.md` (v1.1.0)
> **Phase:** 5.7 (after Protocol 26 Proceed Brief)

## Rule

**You run commands; the human does not substitute for your shell.**

1. Run gates, smoke, **dev servers**, and device reload steps in-session.
2. Report **command + exit code** — never end with “run locally” / “focus your terminal” without trying §9 first.
3. Metro/Expo: start in **background**; probe with `curl`/`lsof`; Android via **`adb`** — not “press r/a” as the only step.
4. Blocked: complete **diagnosis ladder D1–D6**, then **Permission Unblock Report** — re-run when unblocked.

## Diagnosis ladder (before asking human)

D1 Shell → D2 background Shell → D3 node child_process → D4 owner-repo cwd → D5 ecosystem scripts → D6 Unblock Report → D7 human (rare)

## Forbidden (never substitute operator for your shell)

- “Verify locally” / “run in your terminal” / “run these commands”
- **“Your action needed”** / **“Run this sequence”** / numbered paste-only runbooks with no in-session attempt
- **“operator —”** steps you did not try (`aws`, `kubectl`, `terraform`, `pnpm` scripts in repo)
- “Focus the Cursor terminal” / “press a/r in Metro” (without adb/API attempt)
- “I can't send keystrokes” (without adb reload attempt)
- “Let me know when you've run …”
- “Correction noted. P22 directs; I do not poll” **then** dumping commands for the human (contradiction)

## Runbook execution order

1. `cd` **owner repo** (e.g. `gtcx-infrastructure` for W2/k8s/terraform).
2. Run **repo scripts first** (`03-platform/scripts/staging/*.sh`, `pnpm` targets in `package.json`).
3. Run raw commands from docs **yourself**; report **each exit code**.
4. If `aws`/`kubectl` denied → **Permission Unblock Report** (D6) — not a copy-paste block for the operator.
5. **Class A only** (human): net-new production secrets, `gh auth` scope upgrades, legal sign-off.

## W2 / staging (gtcx-infrastructure)

See `docs/operations/agent-staging-execution.md` when present. **Do not** parallelize a different story (IR-3.5) while W2-OPS-001 is **Next priority**.

## Ecosystem helpers

- Git push (harness): `pnpm --dir ../bridge-os ecosystem:push-all`
- Normative spec: gtcx-docs Protocol 27 §8–§9
